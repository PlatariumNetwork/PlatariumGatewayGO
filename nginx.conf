# REFERENCE ONLY — do not deploy this file as-is on production.
#
# Host-specific domains, CORS, ports, and cert paths belong in .env.nginx
# (see .env.nginx.example). On Hestia use:
#   scripts/ensure-nginx.sh  +  deploy/nginx/hestia/platarium-gateway.{tpl,stpl}
#
# Example shape (placeholders, not real hostnames):

# server {
#     listen 443 ssl;
#     server_name YOUR_RPC_DOMAIN;
#     # ssl_certificate     ...;   # panel / certbot paths — never commit private keys
#     # location /ws { proxy_pass http://127.0.0.1:WS_PORT/; ... }
#     # location /  { proxy_pass http://127.0.0.1:REST_PORT; ... }
# }
